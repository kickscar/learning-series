# 05 Traffic Management & High Availability / 03 Route 53

Route 53으로 도메인을 ALB에 연결해 "고정된 접근 경로"를 만든다.

## What you will learn

- DNS와 Route 53(Hosted Zone)의 역할
- Record 타입(A, CNAME)과 AWS 리소스 연결을 위한 Alias
- 도메인 NS 위임과 검증 흐름(nslookup)

## Examples

- `Gallery: Route 53 도메인 연결` -> Public Hosted Zone, Alias Record(A), NS 위임, 도메인 접근 검증

## Reference

- Draft: `.cursor/draft/05.03.notion.cursor.md`
- Notion: [Notion 문서]()
