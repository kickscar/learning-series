# Research: 04.04 — [실습] Gallery: user_data 자동화 + Remote Backend 이전

> 조사일: 2026-04-15
> 리서치 대상: `Cloud/Workloads/gallery-spring-boot/` (소스 또는 README.md)
> 기준: Terraform 1.14.x, hashicorp/aws provider ~> 6.0

---

## Gallery 앱 설정 (Ch02와 동일)

| 항목 | 값 |
|------|---|
| 프로파일 | `dev` |
| 포트 | `8080` |
| DB | H2 인메모리 |
| Storage | 로컬 디스크 |

앱 설정 변경 없음. 인프라 변화만 있다.

---

## 인프라 변화

- Ch02 Gallery의 수동 설치를 user_data + templatefile + systemd로 자동화
- State를 S3 remote backend(native locking, `use_lockfile = true`)로 이전
- DynamoDB 사용하지 않음 (TF 1.10+에서 S3 native locking 지원)

---

## user_data 자동화 도입

Ch04 Gallery부터 user_data + templatefile + systemd로 자동 배포.

### user_data.sh 공통 패턴

```bash
#!/bin/bash
set -euo pipefail

APP_DIR=/opt/gallery
REPO_DIR=/home/ec2-user/workspace
JAR_PATH=/opt/gallery/gallery.jar

# 1. 디렉토리 준비
mkdir -p "${APP_DIR}"
chown -R ec2-user:ec2-user "${APP_DIR}"

# 2. git sparse-checkout + 빌드
sudo -u ec2-user bash -lc "
set -euo pipefail
cd /home/ec2-user
rm -rf workspace
git clone --filter=blob:none --sparse https://github.com/kickscar/learning-series.git workspace
cd workspace
git sparse-checkout init --no-cone
git sparse-checkout set Cloud/Workloads/gallery-spring-boot
cd Cloud/Workloads/gallery-spring-boot
chmod +x ./mvnw
./mvnw clean package -DskipTests -Dbuild.finalName=gallery
"

# 3. JAR 복사
cp "${REPO_DIR}/Cloud/Workloads/gallery-spring-boot/target/gallery.jar" "${JAR_PATH}"
chown ec2-user:ec2-user "${JAR_PATH}"

# 4. systemd 서비스 등록
cat >/etc/systemd/system/gallery.service <<EOF
[Unit]
Description=Gallery Spring Boot
After=network.target

[Service]
Type=simple
User=ec2-user
WorkingDirectory=/opt/gallery
ExecStart=/usr/bin/java -jar /opt/gallery/gallery.jar --spring.profiles.active=dev
Restart=always
RestartSec=5
SuccessExitStatus=143

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable --now gallery.service
```

### templatefile 변수 매핑

| templatefile 변수 | Terraform 소스 | 용도 |
|------------------|---------------|------|
| `${profile}` | `var.env` | spring.profiles.active |

### ExecStart

```bash
ExecStart=/usr/bin/java -jar /opt/gallery/gallery.jar --spring.profiles.active=dev
```

---

## 플랫폼 기준

Amazon Linux 2023: 사용자 `ec2-user`, 패키지 관리자 `dnf`, JDK `java-21-amazon-corretto-headless`.
